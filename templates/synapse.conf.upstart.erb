# This file maintained by Puppet!
# synapse

description	"synapse"

respawn

# Backwards compatibility to run as nobody
exec su -s /bin/sh -c 'exec "$0" "$@"' nobody -- synapse --config <%= @config_file %>
