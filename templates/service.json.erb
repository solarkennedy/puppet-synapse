{
  "default_servers": <%= require "json"; JSON.pretty_generate(@default_servers) %>,
  "discovery": <%= require "json"; JSON.pretty_generate(@discovery) %>,
  "haproxy": <%= require "json"; JSON.pretty_generate(@haproxy) %>
}
